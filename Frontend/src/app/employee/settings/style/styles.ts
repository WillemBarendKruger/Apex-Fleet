import { createStyles, css } from "antd-style";

export const useStyles = createStyles({
    updateContainer: css`
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
        padding: 24px;
    `,

});
